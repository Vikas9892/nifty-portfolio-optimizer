# Core data / optimization
numpy
pandas
matplotlib
yfinance
pyportfolioopt
seaborn

# Web framework
fastapi
uvicorn[standard]

# Auth & security
bcrypt
python-jose[cryptography]==3.5.0

# Configuration / validation
pydantic-settings==2.14.2
python-dotenv
email-validator==2.3.0

# Rate limiting
slowapi==0.1.10

# Database — SQLite (dev) + PostgreSQL (prod)
sqlalchemy>=2.0
psycopg2-binary

# Caching
redis[hiredis]

# Background jobs — Phase 8
rq>=1.16

# Task scheduling — Phase 8
apscheduler>=3.10

# Retry & resilience — Phase 8
tenacity>=8.2

# Observability — Phase 8
prometheus-fastapi-instrumentator>=6.1

# Load testing — Phase 8 (dev dependency)
locust>=2.20

# Streamlit UI (optional / legacy)
streamlit
